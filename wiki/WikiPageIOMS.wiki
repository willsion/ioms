#labels ioms,python
= 简介 =

作者：china.hawkli@gmail.com
当前原型完成版本：1.05_2011.01.21.

*当前版本情况介绍*

当前最重要的不是增加功能，而是把现在已知的BUG都消灭掉，由于python2.x的编码问题比较搞，当前至少50%的BUG和编码相关，已经陷在BUG的海洋中，非常头大。
agent的自更新问题还未开始改；
pyDES的问题大致已经定位为pyDES和多线程的问题，但看来暂时没有解决方案，需要花较长时间去研究或寻找替代方案，暂时搁置，慢慢进行中；
psutil检测的问题已经询问了psutil的开发者，目前的效果已经已经正常，可以使用了，算解决。

*已知BUG*
现有BUG的持续修改；
群发命令的BUG，部分命令正常部分失败（问题比较怪，可能要2周）；
部分命令返回时少了一大段结果的BUG（预计1－3天）
对IP地址错误的服务器发送命令后，IOMServer会死掉的BUG（预计1－2天）
UI用的wxTextEditor不能保存的BUG（预计0.5天）
agent自更新BUG解决（1周内）
DES加密的BUG寻找和解决（搁置）

*后续版本计划*

按先后顺序将撰写以下功能：
监控数据图表显示模块化（此项工作量较大，基本模块预计1个月内完成，完全调整估计要2个月）

预计以上功能完成后，版本号应该在1.1x左右，春节前后应该可以正式试运行。
其后，根据此简体中文版开始派生英文版，并会考虑出big5版本。

*计划时间表：*

2010年12月，完成1.0版本开发原型，目前已经在测试环境运行1个多月，原型正在修复BUG中。

2011年3－4月前后放出第一个编译版本，支持对大量windows 2003服务器的集中化管理（windows 2003简体中文版，其他语言版本存在少数命令不兼容可能），后续版本将会优化windows 2003全语言版本，并会开发linux版本。

2011年9月－12月之间，代码完全开源。由于这个项目与我的研究生论文有关，因此开源时间与论文答辩完成的日期有直接关系，勿催。

*开发语言和工具*

当前程序使用python2.5.4开发，存在cli命令行模式和gui图形模式二个客户端表现形式，gui使用wxpython开发。
数据库使用mysql 5.1，必须使用utf-8编码。
程序使用了以下python扩展模块：WMI,pyDES,Mysqldb,psutil等
windows下使用py2exe打包，linux下计划使用pyinstall打包。


= 正文 =

*IOMS系统设计目标：*

可以对管理区域内安装windows操作系统的服务器下达指定的控制命令，如更改密码，返回CPU内存使用情况，更新指定程序，定时运行程序，守护指定程序运行等；可以对一至多台或全部服务器下达命令并得到返回；定时回收监控数据及下达命令的结果信息，集中后保存在中心数据库中；对数据进行分析，并提取数据产生可供运维决策分析的报表。

*IOMS系统架构*

集中运维管理系统采用CS结构，在目标服务器上安装监控端，并由监控端负责收集和监视系统的各类信息，将信息根据要求发送给集中管理服务器端，集中管理服务端有多项功能模块，并负责接收客户端的数据并记录入数据库、接收管理员的管理命令将命令发至客户端执行并得到返回。
（略）

*IOMS模块介绍*

（略）

*IOMS安装部署说明*

（略）

*关于作者*

（略）